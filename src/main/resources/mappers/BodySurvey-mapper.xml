<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="BodySurvey">

<insert id="insertSurvey">
    INSERT INTO BODY_SURVEY (
        SEQ,
        MEMBER_ID,
        GENDER,
        ANSWER_Q1,
        ANSWER_Q2,
        ANSWER_Q3,
        ANSWER_Q4,
        ANSWER_Q5,
        ANSWER_Q6,
        ANSWER_Q7,
        
        BODY_TYPE,  CREATED_AT
    ) VALUES (
        BODY_SURVEY_SEQ.NEXTVAL,
        #{member_id},
        #{gender},
        #{answer_q1},
        #{answer_q2},
        #{answer_q3},
        #{answer_q4},
        #{answer_q5},
        #{answer_q6},
        #{answer_q7},
        #{body_type}, SYSDATE
    )
</insert>

    <!-- 체형 타입 + 성별로 스타일 추천 조회 -->
<select id="findBodyTypeInfo" resultType="map">
    SELECT
        SUMMARY,
        TOP_TIPS,
        BOTTOM_TIPS,
        OUTER_TIPS,
        PATTERN_TIPS,
        IMAGE_URL  FROM BODY_TYPE
    WHERE BODY_TYPE = #{body_type}
      AND GENDER = #{gender}
</select>

</mapper>