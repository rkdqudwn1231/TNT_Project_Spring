<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="StyleRecommend">

<select id="getRecommendList" parameterType="map" resultType="com.tnt.project.dto.StyleRecommendDTO">
    SELECT *
    FROM BODY_STYLE_RECOMMEND
    WHERE BODY_TYPE = #{body_type}
    AND GENDER = #{gender}
    AND CLOTH_TYPE = #{cloth_type}
    ORDER BY CREATED_AT DESC
</select>
    
</mapper>