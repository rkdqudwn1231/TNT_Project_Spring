<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">

    <!-- 아이디로 회원 조회 -->
    <select id="findByUserId" parameterType="string" resultType="com.tnt.project.dto.MemberDTO">
        SELECT
            id,
            password,
            nickname,
            name,
            gender,
            birth,
            phone,
            email,
            profile_image_uuid     AS image_uuid,
            profile_image_original AS image_original,
            profile_image_url      AS image_url,
            personal_color,
            body_shape,
            created_at,
            updated_at
        FROM member
        WHERE id = #{id}
    </select>

	   <!-- 아이디 중복 -->
    <select id="checkId" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE id = #{id}
    </select>

    <!-- 닉네임 중복 -->
    <select id="checkNickname" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM member
        WHERE nickname = #{nickname}
    </select>
    
	
    <!-- 회원가입 INSERT -->
   <insert id="insertMember" parameterType="com.tnt.project.dto.MemberDTO">
    INSERT INTO member(
        id,
        password,
        nickname,
        name,
        gender,
        birth,
        phone,
        email,
        profile_image_uuid,
        profile_image_original,
        profile_image_url,
        personal_color,
        body_shape,
        created_at,
        updated_at
    )
    VALUES(
        #{id},
        #{password},
        #{nickname},
        #{name},
        #{gender},
        #{birth},
        #{phone},
        #{email},
        #{image_uuid,     jdbcType=VARCHAR},
        #{image_original, jdbcType=VARCHAR},
        #{image_url,      jdbcType=VARCHAR},
        #{personal_color, jdbcType=VARCHAR},
        #{body_shape,     jdbcType=VARCHAR},
        SYSTIMESTAMP,
        SYSTIMESTAMP
    )
</insert>

	<select id="findIdByEmail" parameterType="string" resultType="string">
    SELECT id
    FROM member
    WHERE email = #{email}
	</select>

	<update id="updatePassword" parameterType="map">
    UPDATE member
    SET password = #{password},
        updated_at = SYSTIMESTAMP
    WHERE email = #{email}
	</update>


    <!-- 마이페이지 수정 -->
    <update id="updateMyPage" parameterType="com.tnt.project.dto.MemberDTO">
        UPDATE member
        SET
            nickname              = #{nickname},
            phone                 = #{phone},
            personal_color        = #{personal_color, jdbcType=VARCHAR},
            body_shape            = #{body_shape, jdbcType=VARCHAR},
            profile_image_uuid     = #{image_uuid, jdbcType=VARCHAR},
            profile_image_original = #{image_original, jdbcType=VARCHAR},
            profile_image_url      = #{image_url, jdbcType=VARCHAR}
        WHERE id = #{id}
    </update>

	<update id="updateBodyShape" parameterType="map">
   		UPDATE member
   		SET body_shape = #{body_shape}
  	  	WHERE id = #{id}
	</update>
 
</mapper>
